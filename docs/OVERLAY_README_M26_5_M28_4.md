# M26.5-M28.4 overlay

Apply after the validated and committed M24.5-M26.4 block.

The package integrates worker receipts into the Evidence Store, adds runtime field-qualification bundles, a shell-free supervisor plan, a bounded verification scheduler, and an adaptive coordinator decision loop.

Validate with:

```powershell
.\scripts\validate.ps1
.\scripts\field-tests\m26.5-m28.4.ps1
```
