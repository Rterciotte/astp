# Overlay M22.5-M24.4

Apply over the validated M20.5-M22.4 tree. This overlay intentionally separates buildable worker packaging from operational readiness. Extract, run `scripts/validate.ps1`, then run `scripts/field-tests/m22.5-m24.4.ps1` before committing.
