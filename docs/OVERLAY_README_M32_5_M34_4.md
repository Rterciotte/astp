# M32.5–M34.4 — Runtime field bridge

This block turns runtime qualification from a checklist into a digest-bound, authorized-lab
recording model while preserving the invariant `Planner -> Policy -> Permit -> Worker -> Evidence`.

It does **not** mark bundled images as field-qualified and does not enable network by default.
