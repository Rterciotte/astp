# ASTP M12.7-M14.4 overlay

Apply this ZIP over the committed M11.1-M12.6 repository state.

Then run:

```powershell
.\scripts\validate.ps1
.\scripts\field-tests\m12.7-m14.4.ps1
```

No target network execution is performed by the field harness.
