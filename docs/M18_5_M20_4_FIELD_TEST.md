# M18.5-M20.4 field test

Run:

```powershell
.\scripts\field-tests\m18.5-m20.4.ps1
```

The harness is network-free. It runs the focused tests and displays verifier depth, worker runtime qualification, pentest readiness, and the built-in verifier catalog.

Expected final line:

```text
M18.5-M20.4 FIELD TEST PASSED
Network execution: NOT PERFORMED
```
