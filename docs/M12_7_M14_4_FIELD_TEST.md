# M12.7-M14.4 field test

Run:

```powershell
.\scripts\field-tests\m12.7-m14.4.ps1
```

The harness is offline. It validates focused tests, current assessment coverage, browser/external adapter safety ceilings, pentest readiness, and explicit completion status. It does not resolve secrets and performs no target network requests.
