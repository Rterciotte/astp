# M47.0 — Beginner Workflow and Local Doctor

M47.0 makes the existing engine easier to operate without weakening policy boundaries.

- replaces the temporary overlay README at repository root with a nontechnical usage guide;
- documents the main CLI commands in recommended workflow order;
- adds a complete main-command reference;
- documents specialized field/preflight module entry points separately;
- adds `doctor`, an offline local-environment check;
- reaffirms that milestone change notes belong in `docs/release/`.
