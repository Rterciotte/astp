# M48.3 — CTF Web/API Permit Path

Added `ctf-observe-http`. A challenge must allow automation, use `declared_endpoints_only`, and the GET/HEAD target must exactly match a declared endpoint. The command then reuses the standard ASTP Engagement/Test/signed-permit observation boundary, including lifecycle consumption, rate limiting, evidence manifest, and audit chain. Endpoint declaration alone never authorizes execution.
