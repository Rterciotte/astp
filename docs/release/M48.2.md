# M48.2 — CTF Isolated Solver Adapters

Added `ctf-solve-local` and built-in byte-oriented adapters for text/pattern scanning, printable strings, JSON structure, and ZIP inventory. The workflow enforces local size/entry/candidate bounds, spawns no external process, exposes no unrestricted shell, performs no network, and fails closed when challenge rules prohibit automation.
