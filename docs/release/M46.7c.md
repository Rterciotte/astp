# M46.7c — Priority Queue + JS Offline CLI Bridge

Closes two field-integration gaps without widening authorization.

- Priority ordering applies only to `AUTHORIZABLE` observation-plan items.
- A high score can never promote a blocked/rejected item.
- The existing JavaScript static analyzer is exposed through `python -m astp.javascript_static_cli`.
- Both bridges are offline: no permits and no network execution.

Validation:

```powershell
ruff check . --fix
black .
ruff check .
pytest
.\scripts\validate.ps1
.\scripts\field-tests\m46.7c.ps1
```

After a future separately authorized JS retrieval:

```powershell
python -m astp.javascript_static_cli .\path\artifact.js --output .\path\javascript-static-analysis.yaml
```
