# M48.1 — CTF Artifact Classifier + Hypothesis Graph

Added `ctf-analyze`. It classifies declared local challenge artifacts using local bytes/extensions/magic, records SHA-bound classifications, selects eligible bounded adapters, and creates deterministic hypotheses. Network-capable hypotheses are planning records only and explicitly require a fresh permit. No network is performed.
