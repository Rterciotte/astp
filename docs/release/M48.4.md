# M48.4 — CTF Flag Candidate Verification + Solve Trace

Added `ctf-verify-flags`. Candidates remain bound to artifact path, SHA-256, and adapter provenance and are checked against the challenge's declared flag pattern. Verification events extend the solve trace for reproducibility. A local format match is not represented as proof that a remote competition submission succeeded.
