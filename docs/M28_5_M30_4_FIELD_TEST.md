# M28.5-M30.4 field test

The harness is intentionally offline. It validates bundled runtime declarations, permit-before-I/O ordering, redirect reauthorization, adaptive session decisions, bundle integrity, and conservative full-pentest readiness.

Expected final lines:

```text
M28.5-M30.4 FIELD TEST PASSED
Network execution: NOT PERFORMED
```
