# M4.0 — Bounded Surface Mapper

Builds a capped route/query-key surface map from the target registry. No crawling or network activity occurs.

## Invariant

`Planner -> Policy -> Execution Permit -> Adapter/Worker -> Evidence` remains mandatory.
