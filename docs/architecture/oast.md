# OAST

OAST bindings include payload, program, target, action, permit, detector and issue time. Only a callback matching the exact payload and occurring after issuance can support confirmation. The local fake provider exists for deterministic tests.
