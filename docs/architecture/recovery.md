# Orchestrator recovery

Checkpoints follow critical transitions. Resume revalidates durable state and requires fresh planning and permits. `unknown_outcome` blocks automatic replay. Interim and partial reports are labelled separately from final reports, and the campaign manifest hashes final status and report artifacts.
