# M32.5–M34.4 field harness

The bundled PowerShell harness is intentionally offline. It validates contracts, negative gates,
CLI surfaces, and acceptance semantics. It does not qualify a physical runtime.

A future real qualification run must use an explicitly authorized lab, exact image digests, fresh
permits, and evidence produced by the actual isolated workers.
