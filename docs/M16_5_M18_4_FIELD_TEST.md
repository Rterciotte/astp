# M16.5-M18.4 field test

Run `./scripts/field-tests/m16.5-m18.4.ps1` from PowerShell. The harness runs focused tests and prints the verifier catalog, capability matrix, runtime isolation policy, coverage, and pentest readiness. It performs no target network execution.
