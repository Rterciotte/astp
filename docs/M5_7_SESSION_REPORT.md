# M5.7 — Session Execution Summary

`session-report` combines the durable session ledger with the execution trace and emits a YAML summary of reserved actions/requests, completions, errors, permits issued, and evidence records.
