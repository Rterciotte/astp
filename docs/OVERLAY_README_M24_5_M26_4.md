# M24.5-M26.4 overlay

Apply this overlay after the validated M22.5-M24.4 block.

The package introduces bounded command compilation and executable worker bridges. It does **not** mark Playwright, Nmap, Nuclei, or ZAP as operationally qualified.

Validate with:

```powershell
.\scripts\validate.ps1
.\scripts\field-tests\m24.5-m26.4.ps1
```
