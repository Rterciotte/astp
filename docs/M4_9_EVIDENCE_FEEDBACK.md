# M4.9 — Evidence Feedback Pipeline

`feedback-evidence` converts previously stored HTTP evidence into deterministic discovery candidates and merges them into the target registry with provenance. No candidate becomes executable merely because it was observed.
