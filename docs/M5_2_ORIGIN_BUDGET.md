# M5.2 — Per-Origin Budget

In addition to global session budgets and engagement RPS, a controlled session can cap the number of actions sent to each canonical origin. This prevents queue concentration on one host.
