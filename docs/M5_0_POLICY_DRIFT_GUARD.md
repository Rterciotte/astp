# M5.0 — Policy Drift Guard

A session snapshot stores the current engagement/test digest. Any later mismatch is a hard stop requiring re-planning. The digest is the same policy digest used by execution permits.
