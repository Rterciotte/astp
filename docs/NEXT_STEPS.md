# Next steps

After M42.5-M44.4, physically run the adaptive local-lab assessment and verify the resulting trace against the immutable evidence manifest. The physical run must demonstrate distinct permits, distinct evidence, exact target binding, and state-changing zero-launch without exact approval.

Only after that field gate should ASTP add bug-bounty program portfolio orchestration: platform connectors/catalog refresh, per-program policy snapshots, scope compilation, online/offline attestation, bounded scheduling, per-program evidence/report isolation, and unattended-session stop conditions. A public program must never be tested merely because it was discovered; testing requires a current reviewed/compilable policy and an execution-eligible program state.
