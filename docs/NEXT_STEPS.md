# Next steps after M6.8

M5.9–M6.8 implement bounded evidence-driven iteration while retaining a hard separation between discovery, authorization, permit issuance, and execution:

- M5.9 session-scoped registry feedback after completed observations;
- M6.0 re-planning from newly discovered SAFE candidates;
- M6.1 depth-aware frontier admission;
- M6.2 durable duplicate-action suppression across sessions;
- M6.3 adaptive HEAD-to-GET escalation only when evidence requires body content;
- M6.4 safe web test catalog for header/cookie/CORS/TLS posture;
- M6.5 proof-specific verification plans;
- M6.6 finding lifecycle and retest integration;
- M6.7 multi-program fair scheduling over bounded sessions;
- M6.8 worker isolation contracts for future containerized adapters.

The next candidate block should harden these contracts for distributed execution: asymmetric permit verification, shared transactional ledgers, image digest allowlists, resource ceilings, and operator-reviewed vulnerability-specific proof verifiers.

State-changing, intrusive, credential-attack, and exploit adapters remain outside autonomous execution until their policy/approval/evidence contracts are separately implemented and field-tested.
