# M3.9 — Result Interpreter

Turns immutable HTTP observation evidence into conservative machine-readable signals without creating vulnerabilities by inference.

## Invariant

`Planner -> Policy -> Execution Permit -> Adapter/Worker -> Evidence` remains mandatory.
