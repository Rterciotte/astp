# M46.6 bounded field assessment

Operational boundary:

`live program preflight -> exact target validation -> one-action queue -> fresh permit -> one GET -> evidence -> offline assessment -> report`

This is a bootstrap field cycle, not broad autonomous scanning. A zero-finding report is a valid outcome. Operator review remains mandatory.
