# M4.5 — Surface Prioritization

Ranks already-discovered in-scope targets using deterministic provenance and topology features, not exploit guesses.

## Invariant

`Planner -> Policy -> Execution Permit -> Adapter/Worker -> Evidence` remains mandatory.
