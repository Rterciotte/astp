# ASTP 1.0 RC local CTF suite

This deterministic suite exercises all declared CTF categories using tiny local artifacts. It is not a benchmark and does not contact the network.

Run:

```powershell
python -m astp.cli ctf-acceptance .\examples\ctf\rc-suite\suite.yaml --output .\ctf-acceptance-m48.6.yaml
```
